<?xml version="1.0" encoding="utf-8"?>
<paths>
    <external-files-path name="path_external" path="."/>
    <root-path name="path_root" path=""/>
    <!--3、对应外部内存卡根目录：Environment.getExternalStorageDirectory()   <external-path name="ext_root" path="/" />-->
    <external-files-path name="opensdk_external" path="Images/tmp"/>
    <root-path name="opensdk_root" path=""/>
    <!-- 等同于 Context.getExternalFilesDir(null)-->
    <external-files-path name="share_files" path="." />
    <external-path name="ext_root" path="picture/" />
    <external-files-path name="sharedata" path="shareData/"/>
    <!--
  1、name对应的属性值，开发者可以自由定义；
  2、path对应的属性值，当前external-path标签下的相对路径
  比如：/storage/emulated/0/92Recycle-release.apk
  sdcard路径：/storage/emulated/0
  相对路径：/
  -->
    <!--1、对应内部内存卡根目录：Context.getFileDir()-->
    <files-path
        name="int_root"
        path="." />
    <!--2、对应应用默认缓存根目录：Context.getCacheDir()-->
    <cache-path
        name="app_cache"
        path="." />
    <!--3、对应外部内存卡根目录：Environment.getExternalStorageDirectory()-->
    <external-path
        name="ext_root"
        path="." />
    <!--4、对应外部内存卡根目录下的APP公共目录：Context.getExternalFileDir(String)-->
    <external-files-path
        name="ext_pub"
        path="." />
    <!--5、对应外部内存卡根目录下的APP缓存目录：Context.getExternalCacheDir()-->
    <external-cache-path
        name="ext_cache"
        path="." />
</paths>